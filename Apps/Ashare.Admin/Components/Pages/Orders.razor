@page "/orders"
@inject NavigationManager Nav

@* عشير يعتمد الحجوزات (Bookings) لا الطلبات العامة. أعيد التوجيه للصفحة الصحيحة. *@

@code {
    protected override void OnInitialized() => Nav.NavigateTo("/bookings", replace: true);
}
